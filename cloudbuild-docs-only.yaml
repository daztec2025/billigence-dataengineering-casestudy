steps:
  # Step 1: Build docs Docker image
  - name: "gcr.io/cloud-builders/docker"
    id: "build-docs"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/localspark-docs:$COMMIT_SHA"
      - "-t"
      - "gcr.io/$PROJECT_ID/localspark-docs:latest"
      - "-f"
      - "Dockerfile.docs"
      - "."

  # Step 2: Push docs image
  - name: "gcr.io/cloud-builders/docker"
    id: "push-docs"
    args: ["push", "--all-tags", "gcr.io/$PROJECT_ID/localspark-docs"]

  # Step 3: Deploy docs to Cloud Run Service
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    id: "deploy-docs"
    entrypoint: "gcloud"
    args:
      - "run"
      - "deploy"
      - "localspark-docs"
      - "--image"
      - "gcr.io/$PROJECT_ID/localspark-docs:$COMMIT_SHA"
      - "--region"
      - "us-central1"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--port"
      - "8080"
      - "--memory"
      - "256Mi"
      - "--cpu"
      - "1"
      - "--min-instances"
      - "0"
      - "--max-instances"
      - "2"

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - "gcr.io/$PROJECT_ID/localspark-docs:$COMMIT_SHA"
  - "gcr.io/$PROJECT_ID/localspark-docs:latest"
